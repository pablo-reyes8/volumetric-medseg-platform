apiVersion: 1

providers:
  - name: medseg-runtime
    orgId: 1
    folder: MedSeg
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
